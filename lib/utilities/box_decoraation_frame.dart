import 'package:flutter/material.dart';

class BoxDecorationFrame {
  BoxDecoration thinFrame = BoxDecoration(
    border: Border.all(
      width: 0.5,
      color: Colors.grey,
    ),
    borderRadius: BorderRadius.circular(15),
  );
}
